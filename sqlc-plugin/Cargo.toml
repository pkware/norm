[package]
name = "norm"
version = "1.0.0"
authors = ["PKWARE, Inc."]
edition = "2024"

[profile.release]
lto = true

[dependencies]
protobuf = "3.7.2"
protobuf-json-mapping = "3.7.2"

[build-dependencies]
protobuf-codegen = "3.7.2"

[package.metadata]
wasm-target = "wasm32-wasi"
